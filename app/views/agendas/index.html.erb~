<p id="notice"><%= notice %></p>

<h1>Listado de Contactos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Direccion</th>
      <th>Numerotelefono</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @agendas.each do |agenda| %>
      <tr>
        <td><%= agenda.nombre %></td>
        <td><%= agenda.direccion %></td>
        <td><%= agenda.numerotelefono %></td>
        <td><%= link_to 'Mostrar', agenda %></td>
        <td><%= link_to 'Editar', edit_agenda_path(agenda) %></td>
        <td><%= link_to 'Borrar', agenda, method: :delete, data: { confirm: 'Estas Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo', new_agenda_path %>
